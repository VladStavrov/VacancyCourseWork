springdoc:
  swagger-ui:
    configUrl: "/swagger-config.json"
spring:
  cloud:
    gateway:
      globalcors:
        corsConfigurations:
          '[/**]':
            allowedOrigins: "*"
            allowedHeaders: "*"
            allowedMethods:
              - GET
              - POST
              - DELETE
              - PUT
      routes:
          - id: profile-server
            uri: lb://authorization-service
            predicates:
              - Path=/api1/**
            filters:
              - name: AuthenticationFilter

          - id: node-server
            uri: lb://node-service
            predicates:
              - Path=/api/nodes/**

          - id: auth-server-no-filter
            uri: lb://authorization-service
            predicates:
              - Path=/api/identity/**

          #- id: auth-server-registration
          #  uri: lb://authorization-service
          #  predicates:
          #    - Path=/api/identity/registration
          #  filters:
          #    - name: AuthenticationFilter
          #      args:
          #        requiredRole: ROLE_ADMIN
logging:
  level:
    org.springframework.cloud.gateway: TRACE